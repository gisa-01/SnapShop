import { StyleSheet, Text, View } from 'react-native'

const StoryCard = () => {
  return (
    <View>
      <Text>StoryCard</Text>
    </View>
  )
}

export default StoryCard

const styles = StyleSheet.create({})